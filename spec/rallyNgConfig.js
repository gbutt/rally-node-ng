require('angular');

angular.module('rally-ng.config',[])
	.value('rallyNgConfig', {
		// apiKey: ,
		// username: ,
		// password: ,
		// server: 'https://rally1.rallydev.com',
		// apiVersion: 'v2.0,
		// gzip: true,
		// json: true,
		// headers: {},
	});